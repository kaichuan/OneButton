apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'riversids'
            keyPassword 'tslmfjfj'
            storeFile file('/Users/kc/android_keystore.jks')
            storePassword 'tslmfjfj'
        }
    }
    compileSdkVersion 20
    buildToolsVersion '20.0.0'
    defaultConfig {
        applicationId 'org.piscatory.onebutton'
        minSdkVersion 14
        targetSdkVersion 20
        versionCode 1
        versionName '1.0'
        signingConfig signingConfigs.config
    }
    buildTypes {
        release {
            runProguard true
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    productFlavors {
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}
